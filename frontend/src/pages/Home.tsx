import TaskDisplay from "../components/TaskDisplay";
import { Layout } from "../layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <TaskDisplay />
    </Layout>
  );
};

export default Home;
